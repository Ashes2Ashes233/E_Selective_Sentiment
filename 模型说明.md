# E题模型与实现对应

这是一套快速完成比赛任务的实现。核心借鉴是Visual-Selective-VIO的“当前信息与历史状态驱动选择，再进行循环融合”，情感任务的输入、输出、可用性约束与损失均重新设计。

## 问题1的特征与时序对齐

对给定转写文本和音轨，冻结Wav2Vec2声学模型输出逐帧字符对数概率。用CTC扩展状态序列上的动态规划寻找与转写文本一致的最大概率路径。状态允许保持、前进一个状态以及在非空白且不重复字符时跳过空白，回溯得到每个单词区间 `[s_i,e_i]`。不重新预测情感标签，也不使用外部情感数据。

BERT WordPiece的字符偏移与单词字符范围相交，得到词元时间区间。对于位于该区间的声学或视觉帧，采用均值聚合：

`x_i^m = sum_{t in [s_i,e_i)} f_t^m / number_of_frames`。

没有帧落入极短区间时取最近帧；特殊词元和padding置零并保留掩码。CTC无法完成时使用明确标注的近似回退，而不删除样本。最多保留50个WordPiece位置，超出部分记录截断标记，原始文本与时长仍完整保存。

文本特征为BERT最后层768维；声学为MFCC、差分及6类能量/频谱指标，共32维；视觉为人脸区域DCT、HSV、位置、灰度与光流统计，共35维。视觉描述符是情感相关外观运动的粗粒度代理，不是已校准的表情情感分数。

## 问题2的选择性融合

设m分别表示文本、语音、视觉，t为对齐位置。r_t表示非填充位置，a_mt表示当前模态可观测。对各模态投影并归一化：

`z_mt = a_mt * GELU(LayerNorm(W_m x_mt + b_m))`。

语音和视觉在投影前按训练集有效观测的均值与标准差标准化，并截断到[-8,8]。文本通过同一个冻结BERT重新编码，避免附件2与专项集使用不同特征空间。

策略输入为三路z、上一时刻GRU状态h以及可用性a。每个模态输出两个logit，默认软门控：

`g_mt = sigmoid(logit_keep - logit_drop)`，`w_mt = r_t a_mt g_mt`。

三路门控后特征与a拼接进入GRU。padding位置保持历史状态；真实但全缺失的位置通过缺失标记更新状态。可选硬版本使用三个独立的二分类Gumbel-Softmax，不强迫三模态互斥。硬版本推理不采样，采用最大logit决策；若所有可观测模态均关闭，保留得分最高者。

序列隐状态以有效位置上的注意力池化聚合：

`b_t = softmax_t(v^T h_t)`，`u = sum_t b_t h_t`。

分类输出 `p = softmax(W_c u + b_c)`；强度输出 `yhat = 3*tanh(W_r u + b_r)`。注意力和门控仅作为内部行为记录，不直接解释为因果贡献。

训练损失为：

`L = Huber_beta=0.5(yhat,y) + 0.7 * WeightedCrossEntropy(p,c)`。

类别权重为 `sqrt(n / (3 n_c))`，仅由训练标签统计。第一轮25%的样本做连续缺失增强，后续65%；选择1至3种模态，在有效序列上遮挡10%至60%的连续区间。文本在BERT编码前被遮挡，不能由其他输入字段恢复。

验证选择准则同时考虑完整和缺失输入：

`J = (MAE_clean + MAE_missing)/2 + 0.15*(2 - F1_clean - F1_missing)`。

缺失验证使用固定随机种子和40%区间。连续3轮不改善则早停，测试集不参与选择。默认最多8轮、单随机种子；本次选择第4轮模型。

## 问题3的干预解释

固定完整输入预测类别c，对模态m的作用定义为：

`Delta_mc = p_c(x) - p_c(x_without_m)`。

对强度的作用为 `Delta_my = yhat(x) - yhat(x_without_m)`。主要参考模态按类别概率差的绝对值选择；有符号值保留，因此可识别支持或抑制。由于存在模态交互，三个值不强制归一化成总和100%的贡献。

同样对每个模态的连续4位置窗口实施遮挡，重新编码文本、重新执行门控及GRU，得到局部作用分布。选择绝对作用最大的窗口，并利用原视频CTC对齐结果定位文本、音频时间及视频帧。原特征未提供时间戳，跨模态的位置映射仍是需要说明的假设。关键词与时间区间、映射状态均保存在专项CSV和解释卡中。

高影响窗口与同宽随机窗口的比较用于可复核性展示。由于高影响窗口是从候选中选出的最大值，不把该比较当作独立统计证据，也不将模型干预解释为现实因果关系。

## 模块位置

`common.py`负责三种附件接口、掩码、缺失模拟与标准化统计；`model.py`负责策略与融合；`train.py`负责训练、评价及必要的缺失情景分析；`media.py`负责原视频特征与CTC时间对齐；`predict.py`负责两套专项结果和解释；`make_report.py`负责图表、结果说明与50MB打包检查。

原论文及代码来源见README。本次迁移不继承原VIO权重，不引用原论文的精度或节算比例作为本题结果。
